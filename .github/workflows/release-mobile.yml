name: Mobile — Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      tag:
        description: 'Release tag (e.g. v1.0.0)'
        required: true

jobs:
  build-android:
    name: Build Android (.apk)
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v7
        with:
          node-version: '24'
          cache: npm
          cache-dependency-path: '**/package-lock.json'

      - name: Install
        run: npm ci

      - name: Generate Native Android Folder (Prebuild)
        run: npm run prebuild

      - uses: actions/setup-java@v5
        with:
          distribution: 'zulu'
          java-version: '17'
          cache: 'gradle'

      - name: Cache C++ Build Artifacts
        uses: actions/cache@v6
        with:
          path: android/app/.cxx
          key: cxx-cache-${{ runner.os }}-${{ hashFiles('package-lock.json') }}
          restore-keys: cxx-cache-${{ runner.os }}-

      - name: Build APK
        run: npm run build-android

      - name: Create Release
        uses: softprops/action-gh-release@v3
        with:
          tag_name: ${{ github.event_name == 'workflow_dispatch' && inputs.tag || github.ref_name }}
          name: STOCKMACHINE Mobile ${{ github.event_name == 'workflow_dispatch' && inputs.tag || github.ref_name }}
          generate_release_notes: true
          files: android/app/build/outputs/apk/release/*.apk
