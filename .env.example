# Mobile App Environment Variables
# Copy to .env and fill in values. Expo exposes EXPO_PUBLIC_* vars to the JS bundle.

# Backend base URL (no trailing slash)
EXPO_PUBLIC_API_URL=http://localhost:3000/api/v1
